# spring-loadbalancer-registry
Standalone Spring Boot starter library for registering up to 10 unique service instances (by address) and selecting a next instance (round-robin). Includes Flyway/PostgreSQL persistence, JPA, and full Testcontainers coverage + demo REST API
