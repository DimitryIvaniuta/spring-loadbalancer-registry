server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5448/lb
    username: lb
    password: lb
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

loadbalancer:
  max-instances: 10
